{
  "name": "image-to-sound",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.4",
    "sharp": "^0.32.1",
    "fluent-ffmpeg": "^2.1.2",
    "ffmpeg-static": "^4.4.0"
  }
}
